[package]
name = "terminal-wikipedia"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
reqwest = { version = "0.12", features = ["blocking"] }
scraper = "0.22"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }